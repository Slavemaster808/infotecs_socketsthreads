# Test task infotecs C++

## Usage

### Build
    cmake --preset release
    cmake --build --preset release

### Run client/server
    ./build/release/bin/appcl
    ./build/release/bin/appserv

### Run tests
    ctest --preset release